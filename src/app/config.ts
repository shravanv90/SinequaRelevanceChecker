export class Config {
  app_name: string = '<app name>';
  query_webservice_name: string = '<queryname>';
  username: string = '<user>';
  password: string = '<passw>';
  api_url: string = 'http://<hostname>/api/v1/';
  dataset_url: string =
    'http://<hostname>/api/v1//search.dataset/<dataset query name>';
}
